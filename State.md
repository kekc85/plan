- 2026-08-25:
  - Добавлено всплывающее окно скачивания руководства пользователя (`DownloadManualModal`) с поддержкой выбора пути сохранения на компьютере (через нативный проводник Windows) и быстрым скачиванием без перехода со страницы.
  - В документ Word (`Руководство_пользователя_AeroPlan.docx`) добавлена расширенная коллекция подробных цветных скриншотов всех элементов интерфейса (часы, пресеты AviaBit, строка рейса, чек-боксы, передача смены).
  - Добавлена карточка «Особые замечания по смене»: замечания, введённые при передаче дежурства, теперь отображаются у принимающего сменщика прямо над таблицей с указанием ФИО сдавшего, точного времени и кнопкой «Ознакомлен».
  - Создан и подключен стильный авиационный фавикон (`favicon.svg`, `favicon.ico`): реактивный лайнер на взлете с координатной сеткой центровки и неоновым инверсионным следом.
  - Исправлено отображение города маршрута: в колонке «Маршрут» сверху строго указывается город ПРИЛЁТА (аэропорт назначения, например `REN-MRV` -> `Мин.Воды`, `AER-NJC` -> `Нижневартовск`, `SVO-SUI` -> `Сухум`, `CXR-OVB` -> `Новосибирск`).
  - Исправлена смена даты при переходе через 00:00 (МСК): рейсы после полуночи (например 0:10) теперь получают корректную дату следующих суток (27.08 вместо 26.08).
  - Исправлена подгрузка на хостинге Beget: устранен конфликт параметров авиакомпаний (`nordwind`/`ikar`), подключены постоянные сессионные cookie для прямого получения расписаний без повторного логина и блокировок 429.
  - Очистка рейсов при переключении смен: алгоритм SmartMerge обновлен — при подгрузке расписания на следующие сутки или на сегодня рейсы чужой/прошлой смены больше не прикрепляются снизу вперемешку, а полностью очищаются. Для рейсов текущей подгрузки сохраняются все внесенные диспетчером данные (заметки, топливо, статусы).
  - Усилена фильтрация резервов (`~РЕ307д`, `~`, `~РЕ`, `РЕЗ`, `REZ`, `isSpecialFlight`) и добавлена авто-очистка базы данных.
  - Удалено дублирующее поле ФИО диспетчера в панели управления (оставлен единый профиль в правом верхнем углу).
  - Учетная запись #2 (логин `dispatcher`) в БД автоматически мигрирована с системного описания на реальное имя диспетчера «Иван Иванов».
  - Плейсхолдеры в окне добавления пользователя заменены на наглядные примеры («Иванов Иван Иванович», «ivanov»).
  - Скругление строк: таблица переведена на `border-separate border-spacing-y-1` с плавными радиусами `rounded-l-2xl` и `rounded-r-2xl` и аккуратным огибанием красной рамки просрочки.
  - Окно AviaBit: добавлена яркая индикация выбранного пресета смены.
  - Исправлен локальный парсинг AviaBit (устранена ошибка временных зон и аргументов вызова клиента).
  - Собрана сборка и обновлен архив `plan.zip`.
- 2026-08-24: Исследована структура API сайтов Nordwind (aviabit.nordwindairlines.ru) и Икар (aviabit.ikar.aero).
- Реализована мульти-серверная архитектура с объединением расписаний Nordwind + Икар в один суточный план.
- Добавлена поддержка 2FA-кодов подтверждения из почты:
  - Автоматический диалог ввода 4-значного кода в GUI и консоли.
  - Долговременное кэширование авторизационной сессии Икар в `.session_ikar.json`.
- Доработана логика по замечаниям:
  1. PAX, NOTES: считается сумма первых двух чисел (взрослые + РБ), РМ (инфант без места) не учитывается.
  2. Экипаж: формат строго 4 цифры через слеш (Летный/Бортпроводники/ИТС/Пассажиры), в шапку добавлена расшифровка первых букв: `Экипаж\nЛ/Б/И/П`.
  3. Маршрут: город ПРИЛЁТА сверху (согласно коду IATA из расширенного справочника или динамически из ответа сервера `preliminary[1]["airportName"]`), DEP-ARR снизу (например Казань\nSUI-KZN, Москва\nSUI-SVO, Сыктывкар\nAER-SCW).
  4. Топливо: ячейка остается пустой с сеткой под ручной ввод диспетчером.
  5. Шапка таблицы: выделена жирными (medium) границами со всех сторон каждой ячейки. Колонки СЗВ и ЛДМ — с вертикальной ориентацией текста (90 градусов).
  6. Страница печати Excel: альбомный лист А4, минимальные верхние/нижние/боковые поля (0.2-0.25 in), отключено вертикальное центрирование (таблица начинается сразу сверху как на образце), авто-ширина без сжатия в миниатюру.
  7. Шрифты: основной текст таблицы и шапка — Calibri 11 (рейс обычным шрифтом, PAX жирным), колонка маршрута с городами — Calibri 9.
  8. Защита от блокировки файлов Excel: при открытом файле в Microsoft Excel парсер автоматически сохраняет копию с суффиксом `(1)`, `(2)` и сообщает точный путь без сбоя программы.
  9. Числовые ячейки: Номер ВС, Компановка и PAX записываются в Excel как целые числа (`int`), что полностью убирает зеленые треугольники предупреждений «Число сохранено как текст».
  10. Иконка приложения: создана стильная авиационная иконка `app_icon.ico` (самолет над неоновой таблицей расписания) и подключена в GUI окно и панель задач Windows.
  11. Файл запуска с иконкой: создан ярлык Windows `Суточный план Диспетчера.lnk` с кастомной иконкой самолета для запуска программы в один клик без консоли.
  12. Выбор пути сохранения: при нажатии кнопки выгрузки открывается стандартное окно Windows «Сохранить как...» с предзаполненным именем файла, позволяющее выбрать любую папку (Рабочий стол, Документы, диск и т.д.).
  13. Правило для вылетов из SVO: рейсы с пунктом отправления SVO выгружаются ТОЛЬКО если количество пассажиров равно 0 (пустые/технические рейсы). Рейсы из SVO с пассажирами автоматически отсеиваются. Все остальные 25 аэропортов выгружаются в полном объеме.
  14. Актуальное Московское время вылета (ATD/ETD МСК): колонка «Время» и хронологическая сортировка таблицы теперь используют актуальное время по Москве (UTC+3) с учетом задержек (`dateTakeoffReal` -> `dateTakeoffCalculation` -> `dateTakeoff`).
  15. Выбор диапазона времени: в GUI и парсер добавлена возможность выбора времени начала (ЧЧ:ММ) и окончания (ЧЧ:ММ) по Москве. Рейсы фильтруются строго по указанному временному окну.
  16. Исключение резервов: добавлены строгие правила отсеивания любых служебных записей резерва (с красной буквой R, `~РЕЗ`, `isSpecialFlight`). В итоговую таблицу попадают исключительно рабочие рейсы.
  17. Выпадающий интерактивный календарь: в GUI текстовые поля дат заменены на выпадающие календари `DateEntry` (русская локализация `ru_RU`, формат `ДД.ММ.ГГГГ`), позволяющие выбирать даты кликом мыши по дням и месяцам.

### Правила проекта:
- Язык коммитов в Git: строго русский язык.
- Никаких костылей и фолбэков — только полноценный production-код.
- Все важные изменения фиксируются в State.md.
- Сервисы запускаются в отдельных терминалах/демонах.

  18. Быстрая замена значений: для всех полей даты и времени настроено автоматическое выделение содержимого при фокусе для мгновенной перезаписи.
  19. Авто-маски ввода: подключено автоматическое форматирование для ввода времени (авто-вставка двоеточия `ЧЧ:ММ`) и всеядное распознавание дат (как с точками, так и сплошных цифр `25082026`).
  20. Исправление выпадающего календаря: устранена блокировка кликов по стрелочке DateEntry — выпадающий календарь открывается мгновенно, без задержек и ошибок.
  21. Установка скилла: успешно установлен скилл `frontend-design` в `.agents/skills/frontend-design/` для разработки нового визуального интерфейса.
  22. Веб-приложение «Электронный журнал смены»: в изолированной подпапке `web_app/` создано современное автономное веб-приложение на React 18 + Vite + Tailwind CSS.
  37. База данных MySQL (хостинг Beget) и система учётных записей:
      - Создан файл дампа `schema.sql` для моментального создания таблиц (`plan_users`, `plan_shifts`, `plan_flights`, `plan_handover_logs`) в MySQL на Beget через phpMyAdmin.
      - Реализована безопасная JWT-аутентификация с ролями: Администратор (`admin`) и Диспетчеры (`dispatcher`).
      - Разработан интерфейс администрирования (`AdminModal.jsx`) и мастер передачи смены (`HandoverModal.jsx`).
      - Внедрено умное слияние расписаний (Smart Merge) при подгрузке из AviaBit/Excel с сохранением всей работы сменщика.
  36. Сенсорное перетаскивание рейсов (Touch Drag-and-Drop):
      - Интегрирован `TouchSensor` с задержкой удержания (200мс) для сенсорных экранов смартфонов и планшетов.
      - Быстрые свайпы пальцем используются для плавного вертикального и горизонтального скролла, а удержание пальца на строке рейса активирует перетаскивание.
      - Также на мобильных устройствах доступны быстрые кнопки со стрелками `↑` / `↓` в колонке «Действия» для перемещения рейсов в 1 клик.
  35. Комплексная мобильная оптимизация (Responsive UI):
      - В шапке приложения настроен адаптивный многоуровневый flex-лейаут для смартфонов и планшетов (часы, дата, фильтр смены, поиск и панель действий не перекрывают друг друга и не ломают ширину экрана).
      - В таблице рейсов первые 2 колонки (`#` и `№ Рейса`) сделаны «липкими» слева (`sticky left-0` и `sticky left-8`), что позволяет при горизонтальном скролле на телефонах всегда видеть номер рейса.
      - Карточки статистики адаптированы под 2-колоночную мобильную сетку с симметричным заполнением 5-й карточки.
  34. Исправление ширины колонок «Маршрут» и «Время»:
      - Увеличена ширина ячеек и полей ввода названия городов (`min-w-[125px]`) и даты рейса (`min-w-[90px]`, `w-16`).
      - Длинные названия городов (например «Екатеринбург», «Санкт-Петербург», «Новосибирск») и даты `25.08` теперь отображаются полностью без обрезания.
  33. Полный перевод истории коммитов Git на русский язык:
      - Вся история коммитов в репозитории переписана на чистый русский язык (начиная с первого коммита и до текущего момента).
      - Изменения синхронизированы и отправлены в удаленный репозиторий GitHub.
  32. Фиксация шапки таблицы (Sticky Header):
      - Включена постоянная фиксация заголовка таблицы (`sticky top-0 z-30`) с индивидуальным скроллом строк `max-h-[calc(100vh-175px)]`.
      - При прокрутке любого количества рейсов (20, 30, 50+) названия всех колонок остаются неподвижно закреплены сверху таблицы.
  31. Перетаскивание строк за любое место и динамические версии:
      - Перетаскивание (Drag-and-Drop) включено по всей строке рейса — теперь можно хвататься мышью за любое свободное место строки, метки или ячейки. При этом клики, фокус и ввод текста в `input`, `textarea`, `select` и `button` изолированы через `onPointerDown`/`onMouseDown` и работают безупречно.
      - Версия приложения в подвале переведена на автоматический генератор из Git-коммитов (`git rev-list --count HEAD`) и актуальной даты/времени сборки. При каждом новом коммите версия автоматически возрастает (например, `AeroPlan W&B v1.0.12`).
  30. Логика закрытия рейсов из REN (Оренбург):
      - Для рейсов из REN статус переходит в `Закрыт` (`closed`) строго при установке чекбокса «Времена» (простановка времен в Astra), а не по чекбоксу LDM.
      - При нажатии чекбокса LDM на рейсе REN статус переходит в `Выпущен` (`released`), напоминая о необходимости проставить времена в Astra для окончательного закрытия.
      - Для всех остальных рейсов (не из REN) логика осталась прежней — рейс закрывается по чекбоксу LDM.
  29. Чекбокс «Астра: Времена» для рейсов из REN (Оренбург):
      - Для всех рейсов, вылетающих из Оренбурга (REN), после колонки LDM добавлена интерактивная колонка/чекбокс «Астра (REN) Времена».
      - Позволяет диспетчеру центровки зафиксировать обязательное действие: «проставить вручную время движения и взлёта в Astra после взлёта».
      - Для рейсов не из REN в этой колонке отображается аккуратный прочерк `—`.
  28. Интеллектуальные уведомления и подсветка просроченных рейсов:
      - При перезагрузке страницы, повторном открытии или импорте плана всплывающие окна и звуки для рейсов, чье время выпуска УЖЕ прошло в прошлом, автоматически подавляются (нет спама старыми алертами).
      - Звук и всплывающее окно срабатывают строго в реальном времени в момент наступления времени выпуска (`release_time`).
      - Для всех рейсов с прошедшим временем выпуска, которые ещё не выпущены (не `released`/`closed`), строка в таблице подсвечивается контрастной красной рамкой (`ring-2 ring-rose-500`), бейдж `ВЫП` пульсирует красным цветом.
  27. Исправление сохранения Excel (сохранение приложения открытым):
      - Интегрирован `window.showSaveFilePicker` (File System Access API) и изолированный триггер скачивания.
      - При нажатии на «Экспорт Excel» страница приложения остаётся открытой и активной, поверх неё появляется системное окно выбора папки и имени файла. Вкладка больше не переходит на адрес `blob:...`.
  26. Исправление экспорта Excel:
      - Устранено несоответствие аргументов функции `export_to_excel` (теперь принимает как объекты `datetime`, так и готовую строку диапазона дат `date_str`).
      - Эндпоинт `/api/export_excel` формирует точный файл `openpyxl` со всеми шрифтами, поворотом текста и границами, успешно протестирован и возвращает HTTP 200.
  25. Исправление ввода пробелов во всех текстовых полях:
      - Устранен конфликт с `KeyboardSensor` библиотеки drag-and-drop `@dnd-kit`, перехватывавший нажатия `Space` на уровне строки.
      - Слушатели DnD перенесены строго на иконку перетаскивания (первая колонка `#`), на все поля ввода и текстовые области (`textarea`) добавлены изоляторы `onKeyDown={e.stopPropagation()}`.
      - Теперь во всех полях («Примечания», «Багаж», «Груз», «Почта», «Город» и т.д.) пробелы и многословный текст вводятся свободно и естественно.
  24. Безопасность и выгрузка на GitHub (репозиторий https://github.com/kekc85/plan):
      - Создан локальный секретный файл `.env` для учетных данных AviaBit; создан шаблон `.env.example`.
      - Настроен `.gitignore` (исключены `.env`, сессии, `node_modules`, `dist`, `__pycache__`, `*.xlsx`, `.vscode`).
      - В коде `parser.py` и `api_server.py` удалены открытые пароли, подключена безопасная автозагрузка из `.env`.
      - Добавлен футер и счетчик версий: `AeroPlan W&B v1.0.1 (25.08.2026) • Разработчик: Andrey Zubkov` (`web_app/src/version.js`, `Footer.jsx`).
      - Инициализирован независимый локальный Git-репозиторий в `Parser Aviabit`, настроен remote origin на `https://github.com/kekc85/plan.git`, все коммиты успешно отправлены на GitHub (`git push -u origin main`).
  23. Обновление веб-журнала:
      - Текущая дата в шапке: бейдж подписан как «ЧИСЛО (МСК): 25.08.2026», подчеркивая расчет по Московскому часовому поясу.
      - Авто-маска ввода даты: добавлена функция `formatValidDayMonth` (при вводе цифр `2608` автоматически форматирует в `26.08`, валидирует день 01-31 и месяц 01-12).
      - Ускорение парсера: пул параллельных потоков увеличен до 30 потоков (`max_workers=30`) с тайм-аутом 5 сек, что устранило любые зависания при дозагрузке оперативных данных пассажиров/экипажей по 40+ рейсам.
      - Формат и расположение даты: число и месяц (формат `25.08`) вынесены строго под время вылета в ячейке «ВРЕМЯ»; при выгрузке из парсера AviaBit сразу проставляется корректная точная дата каждого рейса (`DD.MM`).
      - Автоматическая сортировка: при изменении времени вылета рейса (`ВЫЛ`) таблица моментально и плавно пересортировывает строки строго в хронологическом порядке с учетом суточной смены.
      - Звуковое оповещение: сигнал проигрывается строго один раз при появлении окна напоминания (Ding-Dong), без повторов и пищания.
      - Тема оформления: по умолчанию установлена чистая светлая тема (`isDark: false`), с сохранением пользовательского выбора в `localStorage`.
      - В шапке время суточной смены зафиксировано: «09:00 - 09:00».
      - Кухня по умолчанию: строго зафиксирована «D» во всех модулях (новые рейсы, модальное окно, авто-подкачка AviaBit, импорт Excel, демо-данные).
      - Ячейка Маршрута: текст города прилета и коды аэропортов DEP-ARR отцентрированы строго по середине.
      - Мобильная версия и планшеты (Landscape): оптимизированы отступы, компактные сводные карточки метрик в 1 горизонтальный ряд, подключена плавная кинетическая прокрутка таблицы пальцем (`-webkit-overflow-scrolling: touch; touch-action: manipulation`).
      - Исправлены стрелочки в шапке: у кнопки «Импорт» установлена стрелка вниз (загрузка в журнал), у «Excel» — стрелка вверх (выгрузка наружу).
      - Нативный экспорт Excel 1-в-1: реализован эндпоинт `/api/export_excel` на `openpyxl`, сохраняющий абсолютно все настройки парсера.
      - Интеграция с AviaBit: исправлен и запущен FastAPI REST API сервер (`api_server.py` на порту 8000), напрямую подгружающий все реальные рейсы (проверено на выгрузке 21 рейса Nordwind/Икар).
      - Установлены интервалы по умолчанию: время начала `08:00`, время окончания `14:00` (следующих суток).
      - Кнопка «⚡ AviaBit»: полностью переработан дизайн (чистый лаконичный авиационный стиль без грязного оранжевого фона).
      - Чекбокс «СЗВ»: переработан дизайн (чистый контрастный янтарный бейдж с белым четким текстом и галочкой).
      - В шапку добавлены 2 способа загрузки:
        1) Кнопка «⚡ AviaBit» — модальное окно с выбором интервала дат (ДД.ММ.ГГГГ), времени (08:00 - 14:00), быстрыми пресетами и авиакомпаний.
        2) Кнопка «📁 Импорт» — прямая загрузка любого сформированного файла Excel (.xlsx) с авто-разбором всех колонок.
      - Столбцы упорядочены: «MTOW» (сбалансированная комфортная ширина w-15), затем «LIR».
      - В шапке добавлены двое часов UTC и МСК со свободным разделителем.
      - Ячейка «Время»: 2 поля — сверху время выпуска (по умолчанию -40 минут от вылета, авто-расчет), снизу время вылета.
      - Система умных оповещений: всплывающее окно при наступлении времени выпуска рейса с быстрой кнопкой «Выпустить рейс».
      - Полная двусторонняя синхронизация: выбор статуса проставляет соответствующие чекбоксы (LIR, СЗВ, LDM) и наоборот.
      - Многострочные поля (`textarea`): Багаж, Груз, Почта, Заметки свободно переносят текст в несколько строк.
      - Статус «Закрыт»: четко и контрастно выделяет всю строку в светлой теме (темно-серый контраст) и темной теме (графит).
- Генератор Excel полностью приведен к формату "Суточный план Диспетчера группы центровки" (15 колонок).
- Обновлен GUI (gui.py), веб-журнал (web_app/) и документация (README.md).
